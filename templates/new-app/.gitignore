node_modules/
build/
.bract-image-cache/

# Codegen output — regenerated by `bractjs codegen:registry`
# and `bractjs codegen:manifest`. Required for `bun build --compile` but
# never edited by hand.
app/_generated/
app/route-types.gen.ts

# Bun lockfile is tracked; bun.lock.gz isn't
bun.lock.gz

# Local env
.env
.env.local
