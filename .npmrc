link-workspace-packages=true
prefer-workspace-packages=true
# BractJS ships raw .ts run under Bun; hoisting keeps react/react-dom/@types/*
# resolvable from example dirs the way `file:` linking did.
shamefully-hoist=true
