{
  "name": "demo-cms",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "css": "tailwindcss -i ./app/styles.css -o ./public/styles.css --minify",
    "css:watch": "tailwindcss -i ./app/styles.css -o ./public/styles.css --watch",
    "predev": "bun run css",
    "dev": "bractjs dev",
    "prebuild": "bun run css",
    "build": "NODE_ENV=production bractjs build",
    "start": "NODE_ENV=production bractjs start",
    "precompile": "bun run css",
    "compile": "NODE_ENV=production bractjs compile ./bin/cms-app ./app/server.ts",
    "start:bin": "./bin/cms-app",
    "seed:demo": "bun run scripts/seed-demo.ts",
    "test": "bun test",
    "pretypecheck": "bractjs codegen:seed",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@bractjs/bractjs": "workspace:*",
    "lucide-react": "^1.17.0",
    "nodemailer": "^8.0.10",
    "react": "19.2.6",
    "react-dom": "19.2.6"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "@types/bun": "latest",
    "@types/nodemailer": "^8.0.0",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tailwindcss": "^4.3.0"
  },
  "//": "Pin react/react-dom to one version to avoid a dual-React 'invalid hook call' at hydration for \"use client\" components. In the pnpm workspace, react is hoisted/shared across the workspace-linked framework so a single copy is used; root-level pnpm.overrides enforce the version repo-wide."
}
